# Copy to .env.local for development. Production values are set in
# Cloudflare Pages dashboard → Settings → Environment Variables.
#
# Values match local.properties on the Android side so portal and
# phones talk to the same Supabase project.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
